<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WarehouseManagement" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WarehouseManagement" parent="Base.Theme.WarehouseManagement" />

    <style name="Theme.WarehouseManagement.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.WarehouseManagement.SplashScreen" parent="Theme.WarehouseManagement.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/bg_sc_start</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logo</item>
    </style>

    <style name="Theme.Warehousemanagement.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Warehousemanagement.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    
    <style name="custom_TV">
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="custom_TV_show">
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">@dimen/margin</item>
        <item name="android:textColor">@color/black</item>
    </style>
</resources>